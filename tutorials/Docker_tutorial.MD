# 🐳 Полный гайд по Docker для этого проекта

## Содержание

1. [Что такое Docker и зачем он нужен](#что-такое-docker-и-зачем-он-нужен)
2. [Разбор Docker-файлов проекта](#разбор-docker-файлов-проекта)
3. [Типичный Docker Pipeline](#типичный-docker-pipeline)
4. [Справочник команд Docker](#справочник-команд-docker)
5. [⭐ Когда нужно пересобирать контейнер](#когда-нужно-пересобирать-контейнер)
6. [Лучшие практики](#лучшие-практики)
7. [Решение проблем](#решение-проблем)
8. [Практические примеры для этого проекта](#практические-примеры-для-этого-проекта)

---

## Что такое Docker и зачем он нужен

### Что такое Docker?

**Docker** — это платформа для разработки, доставки и запуска приложений в изолированных контейнерах. Контейнер — это легковесная, автономная среда, которая содержит всё необходимое для работы вашего приложения: код, библиотеки, системные утилиты и настройки.

### Контейнеры vs Виртуальные машины

Представьте себе разницу между контейнером и виртуальной машиной (VM):

**Виртуальная машина:**
- Включает полную операционную систему (ОС)
- Занимает много места (гигабайты)
- Медленно запускается (минуты)
- Большая изоляция, но больше накладных расходов

**Контейнер:**
- Использует ядро хостовой ОС
- Занимает мало места (мегабайты)
- Запускается мгновенно (секунды)
- Лёгкая изоляция с минимальными накладными расходами

### Зачем Docker нужен в этом проекте?

1. **Единообразная среда выполнения**
   На вашем компьютере Python 3.12, у коллеги 3.10, на сервере 3.14. Docker гарантирует, что приложение будет работать одинаково везде — на вашей машине, на сервере, у любого разработчика.

2. **Изоляция зависимостей**
   Все зависимости (FastAPI, httpx, uvicorn) живут внутри контейнера и не конфликтуют с другими проектами на вашей машине.

3. **Простота развёртывания**
   Вместо инструкций "установи Python, создай venv, установи зависимости" вы просто говорите: "запусти `docker-compose up`". Одна команда.

4. **Воспроизводимость**
   Через год вы сможете запустить проект точно так же, как сегодня. Docker-образ фиксирует все версии зависимостей.

5. **Масштабируемость**
   В будущем можно легко запустить несколько экземпляров приложения, добавить базу данных, кеш Redis — всё через Docker Compose.

### Ключевые понятия

- **Image (Образ)** — шаблон, из которого создаются контейнеры. Это как класс в ООП.
- **Container (Контейнер)** — запущенный экземпляр образа. Это как объект класса.
- **Dockerfile** — рецепт создания образа (инструкции для Docker).
- **Docker Compose** — инструмент для управления несколькими контейнерами (оркестрация).

---

## Разбор Docker-файлов проекта

В этом проекте три файла, связанных с Docker:

1. **`dockerfile`** — описывает, как собрать образ приложения
2. **`docker-compose.yaml`** — описывает, как запустить контейнер(ы)
3. **`.dockerignore`** — описывает, какие файлы исключить из сборки

Давайте разберём каждый файл построчно.

---

### 📄 `dockerfile` — Рецепт сборки образа

```dockerfile
# Base image
FROM python:3.12-slim
```

**Что делает:** Указывает базовый образ — отправную точку для нашего образа.
**Почему `python:3.12-slim`:**
- `python:3.12` — официальный образ Python версии 3.12
- `slim` — облегчённая версия (меньше размер, нет лишних пакетов)
- Альтернативы: `python:3.12` (полный), `python:3.12-alpine` (минималистичный, но может быть проблематичнее)

---

```dockerfile
# Working directory inside container
WORKDIR /app
```

**Что делает:** Создаёт директорию `/app` внутри контейнера и делает её рабочей.
**Почему это важно:**
- Все последующие команды (`COPY`, `RUN`) будут выполняться относительно `/app`
- Удобно: вы всегда знаете, где находится ваш код внутри контейнера
- Аналог команды `cd /app` в терминале

---

```dockerfile
# Install dependencies (cached layer)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
```

**Что делает:**
1. Копирует файл `requirements.txt` из вашего проекта в `/app` внутри контейнера
2. Устанавливает все зависимости из `requirements.txt`

**Почему именно в таком порядке:**
- Docker использует **кеширование слоёв (layer caching)**
- Если `requirements.txt` не изменился, Docker пропустит установку зависимостей при пересборке
- Установка зависимостей — долгая операция, кеширование экономит время

**Почему `--no-cache-dir`:**
- Отключает кеш pip, уменьшает размер образа
- pip по умолчанию сохраняет скачанные пакеты, но в образе они не нужны

**Почему сначала `requirements.txt`, а не весь код:**
```
# ❌ Плохо:
COPY . .
RUN pip install -r requirements.txt

# ✅ Хорошо:
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
```

В первом случае при любом изменении кода Docker будет переустанавливать все зависимости.
Во втором случае зависимости переустановятся только при изменении `requirements.txt`.

---

```dockerfile
# Copy application code
COPY main.py .
COPY static/ ./static/
```

**Что делает:** Копирует код приложения в контейнер.
**Почему два `COPY`:**
- Явно показывает, какие файлы нужны приложению
- Можно было бы использовать `COPY . .`, но это менее наглядно

**Что НЕ копируется:**
- Файлы из `.dockerignore` (`.venv/`, `.git/`, `.env` и др.)
- Это экономит место и повышает безопасность

---

```dockerfile
# Expose FastAPI port
EXPOSE 8000
```

**Что делает:** Документирует, что контейнер слушает порт 8000.
**Важно понимать:**
- Это НЕ публикует порт (не делает его доступным снаружи)
- Это просто метаданные для документации
- Фактическая публикация порта происходит через `-p` в `docker run` или `ports` в `docker-compose.yaml`

---

```dockerfile
# Start the app
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**Что делает:** Указывает команду по умолчанию при запуске контейнера.
**Разбор команды:**
- `uvicorn` — ASGI сервер для FastAPI
- `main:app` — запустить объект `app` из файла `main.py`
- `--host 0.0.0.0` — слушать все сетевые интерфейсы (не только localhost)
- `--port 8000` — слушать порт 8000

**Почему `0.0.0.0`, а не `127.0.0.1`:**
- `127.0.0.1` — только внутри контейнера
- `0.0.0.0` — доступно извне контейнера

**Почему `CMD`, а не `RUN`:**
- `RUN` — выполняется при сборке образа (например, установка зависимостей)
- `CMD` — выполняется при запуске контейнера (например, запуск приложения)

**Почему НЕТ `--reload`:**
- `--reload` используется в разработке для автоматического перезапуска при изменении кода
- В контейнере изменения кода требуют пересборки образа, поэтому `--reload` бесполезен
- Для разработки лучше использовать bind mount (будет рассмотрено далее)

---

### 📄 `docker-compose.yaml` — Оркестрация контейнеров

```yaml
version: "3.8"
```

**Что делает:** Указывает версию синтаксиса Docker Compose.
**Почему `3.8`:**
- Современная стабильная версия
- Поддерживает все нужные функции
- Совместима с большинством версий Docker

---

```yaml
services:
  chat-app:
```

**Что делает:** Определяет сервис (контейнер) с именем `chat-app`.
**Почему это важно:**
- Можно определить несколько сервисов (например, `chat-app`, `database`, `redis`)
- Сервисы могут общаться друг с другом по именам

---

```yaml
    build: .
```

**Что делает:** Собирает образ из `dockerfile` в текущей директории (`.`).
**Альтернативы:**
```yaml
# Использовать готовый образ из Docker Hub:
image: python:3.12-slim

# Указать путь к Dockerfile:
build:
  context: .
  dockerfile: custom.dockerfile
```

---

```yaml
    container_name: websandbox-chat-app
```

**Что делает:** Задаёт имя контейнера.
**Почему это удобно:**
- Без этого Docker генерирует случайное имя (например, `websandboxproject_chat-app_1`)
- С этим вы можете писать `docker logs websandbox-chat-app` вместо поиска имени

---

```yaml
    ports:
      - "8000:8000"
```

**Что делает:** Пробрасывает (публикует) порты: `HOST:CONTAINER`.
**Разбор:**
- `8000` (слева) — порт на вашей машине (хосте)
- `8000` (справа) — порт внутри контейнера

**Примеры:**
```yaml
# Доступ: localhost:3000 → контейнер:8000
ports:
  - "3000:8000"

# Несколько портов:
ports:
  - "8000:8000"
  - "5432:5432"  # Например, для базы данных
```

---

```yaml
    env_file:
      - .env
```

**Что делает:** Загружает переменные окружения из файла `.env`.
**Почему это важно:**
- API ключи, секреты не хранятся в коде
- `.env` добавлен в `.gitignore` и `.dockerignore` для безопасности
- Можно использовать разные `.env` файлы для разработки и продакшна

**Альтернатива:**
```yaml
environment:
  - HUGGING_FACE_API_KEY=hf_your_key  # ❌ Плохо: секрет в коде
```

---

```yaml
    restart: unless-stopped
```

**Что делает:** Автоматически перезапускает контейнер при падении.
**Опции:**
- `no` — не перезапускать (по умолчанию)
- `always` — всегда перезапускать, даже если вы остановили вручную
- `unless-stopped` — перезапускать, пока вы не остановили вручную
- `on-failure` — перезапускать только при ошибке (exit code ≠ 0)

**Для этого проекта:**
- `unless-stopped` — хороший выбор для разработки и продакшна
- Контейнер перезапустится при перезагрузке системы или падении приложения

---

### 📄 `.dockerignore` — Что исключить из сборки

```dockerignore
# Python
__pycache__/
*.py[cod]
.Python
```

**Что делает:** Исключает Python кеш-файлы из образа.
**Почему это важно:**
- `__pycache__` — скомпилированные `.pyc` файлы, не нужны в образе
- Уменьшает размер образа
- Ускоряет сборку (меньше файлов для копирования)

---

```dockerignore
# Virtual environments
.venv/
env/
venv/
```

**Что делает:** Исключает виртуальные окружения Python.
**Почему это критично:**
- Виртуальное окружение — это тысячи файлов (сотни мегабайт)
- Они создаются для локальной разработки, в контейнере не нужны
- В контейнере зависимости устанавливаются через `pip install -r requirements.txt`

---

```dockerignore
# Environment variables (never bake secrets!)
.env
```

**Что делает:** Исключает `.env` файл с секретами.
**Почему это критично для безопасности:**
- `.env` содержит API ключи и другие секреты
- Если `.env` попадёт в образ, секреты можно извлечь из образа
- Правильный подход: передавать `.env` через `env_file` в `docker-compose.yaml`

**Опасность:**
```dockerfile
# ❌ НИКОГДА НЕ ДЕЛАЙТЕ ТАК:
COPY .env .
```

Теперь любой, кто получит ваш Docker образ, увидит ваши секреты!

---

```dockerignore
# Git
.git/
.gitignore
```

**Что делает:** Исключает Git репозиторий.
**Почему:**
- `.git/` — история коммитов, может быть огромной
- Не нужна для запуска приложения
- Уменьшает размер образа

---

```dockerignore
# IDE
.idea/
.vscode/
```

**Что делает:** Исключает файлы настроек редакторов.
**Почему:**
- Это персональные настройки разработчика
- Не нужны для работы приложения

---

```dockerignore
# OS files
.DS_Store
Thumbs.db
```

**Что делает:** Исключает системные файлы macOS и Windows.
**Почему:**
- `.DS_Store` (macOS) и `Thumbs.db` (Windows) — служебные файлы ОС
- Не нужны в образе, только засоряют

---

## Типичный Docker Pipeline

Типичный цикл работы с Docker выглядит так:

```
┌─────────────────┐
│  1. Написать    │
│     код         │
└────────┬────────┘
         │
         v
┌─────────────────┐
│  2. Собрать     │
│     образ       │ ← docker build или docker-compose build
└────────┬────────┘
         │
         v
┌─────────────────┐
│  3. Запустить   │
│   контейнер     │ ← docker run или docker-compose up
└────────┬────────┘
         │
         v
┌─────────────────┐
│  4. Тестировать │
│   приложение    │ ← Открыть http://localhost:8000
└────────┬────────┘
         │
         v
┌─────────────────┐
│  5. Остановить/ │
│   перезапустить │ ← docker-compose down/restart
└────────┬────────┘
         │
         v
┌─────────────────┐
│  6. Изменить    │
│     код         │
└────────┬────────┘
         │
         v
     (Повтор)
```

### Детальный процесс

**Шаг 1: Написать код**
- Редактируете `main.py`, `static/script.js` и т.д.
- Вносите изменения в логику приложения

**Шаг 2: Собрать образ**
```bash
docker-compose build
```
- Docker читает `dockerfile`
- Выполняет все инструкции: FROM, WORKDIR, COPY, RUN, etc.
- Создаёт образ — неизменяемый шаблон

**Шаг 3: Запустить контейнер**
```bash
docker-compose up
```
- Docker создаёт контейнер из образа
- Запускает команду из `CMD` (uvicorn)
- Пробрасывает порты, загружает `.env`

**Шаг 4: Тестировать**
- Открываете `http://localhost:8000`
- Проверяете работу приложения
- Смотрите логи в терминале

**Шаг 5: Остановить**
```bash
docker-compose down
```
- Останавливает контейнер
- Удаляет контейнер (но НЕ образ)

**Шаг 6: Изменить код → Пересобрать**
- Вносите изменения
- Пересобираете образ (см. раздел ниже о том, когда это нужно)
- Перезапускаете контейнер

---

## Справочник команд Docker

### Команды сборки

#### `docker build`
```bash
# Собрать образ из Dockerfile в текущей директории
docker build -t my-app .

# -t my-app  →  присвоить имя образу
# .          →  путь к Dockerfile (текущая директория)
```

#### `docker-compose build`
```bash
# Собрать все образы из docker-compose.yaml
docker-compose build

# Собрать конкретный сервис
docker-compose build chat-app

# Пересобрать без использования кеша
docker-compose build --no-cache
```

---

### Команды запуска

#### `docker run`
```bash
# Запустить контейнер из образа
docker run -p 8000:8000 my-app

# Запустить в фоновом режиме
docker run -d -p 8000:8000 my-app

# Запустить с переменными окружения
docker run -p 8000:8000 --env-file .env my-app
```

#### `docker-compose up`
```bash
# Запустить все сервисы
docker-compose up

# Запустить в фоновом режиме (detached mode)
docker-compose up -d

# Пересобрать и запустить
docker-compose up --build

# Пересобрать конкретный сервис и запустить
docker-compose up --build chat-app
```

---

### Команды остановки

#### `docker stop`
```bash
# Остановить контейнер по имени
docker stop websandbox-chat-app

# Остановить по ID
docker stop a1b2c3d4
```

#### `docker-compose down`
```bash
# Остановить и удалить контейнеры
docker-compose down

# Остановить и удалить контейнеры + образы
docker-compose down --rmi all

# Остановить и удалить всё (контейнеры, сети, volumes)
docker-compose down -v
```

#### `docker-compose restart`
```bash
# Перезапустить все сервисы
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart chat-app
```

---

### Команды просмотра

#### `docker ps`
```bash
# Показать запущенные контейнеры
docker ps

# Показать ВСЕ контейнеры (включая остановленные)
docker ps -a
```

**Пример вывода:**
```
CONTAINER ID   IMAGE         COMMAND                  STATUS         PORTS                    NAMES
a1b2c3d4e5f6   my-app        "uvicorn main:app..."    Up 5 minutes   0.0.0.0:8000->8000/tcp   websandbox-chat-app
```

#### `docker images`
```bash
# Показать все образы
docker images

# Показать размеры образов
docker images --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}"
```

**Пример вывода:**
```
REPOSITORY   TAG       SIZE
my-app       latest    187MB
python       3.12-slim 125MB
```

#### `docker logs`
```bash
# Показать логи контейнера
docker logs websandbox-chat-app

# Следить за логами в реальном времени (как tail -f)
docker logs -f websandbox-chat-app

# Показать последние 100 строк логов
docker logs --tail 100 websandbox-chat-app
```

#### `docker-compose logs`
```bash
# Показать логи всех сервисов
docker-compose logs

# Следить за логами
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs chat-app
```

---

### Команды очистки

#### `docker rm`
```bash
# Удалить остановленный контейнер
docker rm websandbox-chat-app

# Принудительно удалить (даже если запущен)
docker rm -f websandbox-chat-app
```

#### `docker rmi`
```bash
# Удалить образ
docker rmi my-app

# Удалить несколько образов
docker rmi my-app python:3.12-slim

# Принудительно удалить (даже если используется)
docker rmi -f my-app
```

#### `docker system prune`
```bash
# Удалить всё неиспользуемое (stopped containers, dangling images, unused networks)
docker system prune

# Удалить ВСЁ неиспользуемое + все образы без контейнеров
docker system prune -a

# Показать, сколько места будет освобождено
docker system df
```

**Пример использования:**
```bash
# Посмотреть, сколько места занято
docker system df

# Результат:
# TYPE            TOTAL     ACTIVE    SIZE      RECLAIMABLE
# Images          10        2         3.5GB     2GB (57%)
# Containers      5         1         100MB     80MB (80%)

# Очистить
docker system prune -a

# Освободится ~2.08GB
```

---

### Дополнительные полезные команды

#### `docker exec`
```bash
# Выполнить команду внутри запущенного контейнера
docker exec websandbox-chat-app ls -la

# Открыть интерактивную оболочку (bash)
docker exec -it websandbox-chat-app bash

# Запустить Python REPL внутри контейнера
docker exec -it websandbox-chat-app python
```

#### `docker inspect`
```bash
# Получить детальную информацию о контейнере в JSON
docker inspect websandbox-chat-app

# Получить IP адрес контейнера
docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' websandbox-chat-app
```

---

## ⭐ Когда нужно пересобирать контейнер

**Это самый важный раздел для понимания рабочего процесса с Docker.**

### Когда НУЖНО пересобирать образ

#### 1. Изменения в коде приложения

**Примеры:**
- Изменили `main.py` (добавили endpoint, исправили логику)
- Изменили `static/script.js` (исправили баг в JS)
- Изменили `static/style.css` (обновили дизайн)
- Изменили `static/index.html` (добавили элементы интерфейса)

**Почему нужно пересобирать:**
- Код копируется в образ командой `COPY main.py .` и `COPY static/ ./static/`
- Изменения в вашем проекте НЕ автоматически попадают в образ
- Нужно заново скопировать файлы в образ

**Как пересобрать:**
```bash
# Остановить контейнер
docker-compose down

# Пересобрать образ и запустить
docker-compose up --build
```

**Пример:**
```python
# main.py - Изменили язык логирования с английского на русский
logging.info("Запрос к Hugging Face API")  # Было: "Requesting Hugging Face API"
```

После такого изменения **обязательно нужна пересборка**.

---

#### 2. Изменения в `requirements.txt`

**Примеры:**
- Добавили новую библиотеку: `pip install pydantic-settings`
- Обновили версию: `fastapi==0.109.0` → `fastapi==0.110.0`
- Удалили зависимость

**Почему нужно пересобирать:**
- Зависимости устанавливаются при сборке образа: `RUN pip install -r requirements.txt`
- Изменения в `requirements.txt` на вашей машине не влияют на образ

**Как пересобрать:**
```bash
# 1. Обновить requirements.txt
pip freeze > requirements.txt

# 2. Пересобрать образ
docker-compose down
docker-compose up --build
```

**Важно:** Если вы добавили библиотеку в локальное окружение (`pip install package`), но забыли обновить `requirements.txt`, контейнер не будет работать.

---

#### 3. Изменения в `dockerfile`

**Примеры:**
- Изменили базовый образ: `FROM python:3.12-slim` → `FROM python:3.14-slim`
- Добавили новую команду: `RUN apt-get update && apt-get install -y curl`
- Изменили `CMD` или `EXPOSE`

**Почему нужно пересобирать:**
- `dockerfile` — это рецепт сборки образа
- Любое изменение рецепта требует пересборки

**Как пересобрать:**
```bash
docker-compose down
docker-compose up --build
```

---

#### 4. Изменения в `.dockerignore`

**Примеры:**
- Добавили новый паттерн: `*.log`
- Убрали паттерн (теперь файлы должны попасть в образ)

**Почему нужно пересобирать:**
- `.dockerignore` влияет на то, какие файлы копируются в образ
- Изменения применяются только при пересборке

**Как пересобрать:**
```bash
docker-compose down
docker-compose up --build
```

---

### Когда НЕ НУЖНО пересобирать образ

#### 1. Изменения в `.env` файле

**Примеры:**
- Изменили API ключ: `HUGGING_FACE_API_KEY=hf_новый_ключ`
- Добавили новую переменную: `DEBUG=True`
- Изменили значение переменной

**Почему НЕ нужно пересобирать:**
- `.env` НЕ копируется в образ (он в `.dockerignore`)
- `.env` загружается при ЗАПУСКЕ контейнера через `env_file` в `docker-compose.yaml`
- Достаточно перезапустить контейнер

**Как перезапустить:**
```bash
# Вариант 1: Полная остановка и запуск
docker-compose down
docker-compose up

# Вариант 2: Просто перезапуск (быстрее)
docker-compose restart
```

**Пример:**
```bash
# Изменили .env
HUGGING_FACE_API_KEY=hf_старый_ключ  →  HUGGING_FACE_API_KEY=hf_новый_ключ

# Перезапустить (БЕЗ пересборки)
docker-compose restart

# Приложение подхватит новый ключ
```

---

#### 2. Изменения в файлах, не копируемых в образ

**Примеры:**
- Изменили `README.md`
- Изменили `CLAUDE.md`
- Изменили `.gitignore`
- Добавили файл в `.venv/`

**Почему НЕ нужно пересобирать:**
- Эти файлы в `.dockerignore`, они не попадают в образ
- Контейнер их вообще не видит

**Действие:**
```bash
# Ничего делать не нужно
# Контейнер продолжит работать как раньше
```

---

#### 3. Изменения в `docker-compose.yaml` (зависит от типа изменения)

**Не требует пересборки:**
- Изменили `container_name`
- Изменили `restart` policy
- Добавили/изменили `ports` (пробросили другой порт)

**Требует пересборки:**
- Изменили `build` context
- Добавили build args

**Как применить изменения (без пересборки):**
```bash
docker-compose down
docker-compose up
```

---

### Сводная таблица

| Изменение                     | Нужна пересборка? | Команда                          |
|-------------------------------|-------------------|----------------------------------|
| `main.py`, `static/*`         | ✅ Да             | `docker-compose up --build`      |
| `requirements.txt`            | ✅ Да             | `docker-compose up --build`      |
| `dockerfile`                  | ✅ Да             | `docker-compose up --build`      |
| `.dockerignore`               | ✅ Да             | `docker-compose up --build`      |
| `.env`                        | ❌ Нет            | `docker-compose restart`         |
| `README.md`, `CLAUDE.md`      | ❌ Нет            | Ничего не делать                 |
| `.gitignore`                  | ❌ Нет            | Ничего не делать                 |
| `docker-compose.yaml` (ports) | ❌ Нет            | `docker-compose up` (без build)  |

---

### Как правильно пересобрать

**Полный процесс:**
```bash
# 1. Остановить и удалить контейнер
docker-compose down

# 2. Пересобрать образ и запустить
docker-compose up --build

# Или сразу в фоновом режиме:
docker-compose up --build -d
```

**Пересборка без кеша (если что-то пошло не так):**
```bash
# Полная очистка и пересборка с нуля
docker-compose down
docker-compose build --no-cache
docker-compose up
```

**Проверка, что новый код работает:**
```bash
# Смотрим логи
docker-compose logs -f

# Проверяем в браузере
curl http://localhost:8000/hello

# Смотрим, какой образ используется
docker images | grep websandbox
```

---

### Объяснение Layer Caching (кеширование слоёв)

Docker кеширует каждую инструкцию в `dockerfile` как отдельный слой.

**Пример:**
```dockerfile
FROM python:3.12-slim          # Слой 1
WORKDIR /app                   # Слой 2
COPY requirements.txt .        # Слой 3
RUN pip install -r requirements.txt  # Слой 4 (долгий!)
COPY main.py .                 # Слой 5
COPY static/ ./static/         # Слой 6
```

**Сценарий 1: Изменили только `main.py`**
```bash
docker-compose build
```

Docker:
- Слои 1-4: Использую из кеша (не изменились)
- Слой 5: Пересобираю (изменился `main.py`)
- Слой 6: Пересобираю (последующие слои инвалидируются)

**Результат:** Сборка быстрая (~5 секунд), установка зависимостей пропущена.

---

**Сценарий 2: Изменили `requirements.txt`**
```bash
docker-compose build
```

Docker:
- Слои 1-2: Использую из кеша
- Слой 3: Пересобираю (изменился `requirements.txt`)
- Слой 4: Пересобираю (переустанавливаю зависимости) ← Долго!
- Слои 5-6: Пересобираю

**Результат:** Сборка медленная (~1-2 минуты), зависимости переустановлены.

---

**Почему это важно:**

Правильный порядок инструкций экономит время:

```dockerfile
# ✅ Хорошо: зависимости кешируются
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY main.py .

# ❌ Плохо: при любом изменении кода переустанавливаются зависимости
COPY . .
RUN pip install -r requirements.txt
```

---

## Лучшие практики

### 1. Минимизируйте количество слоёв

**Плохо:**
```dockerfile
RUN apt-get update
RUN apt-get install -y curl
RUN apt-get install -y wget
```

**Хорошо:**
```dockerfile
RUN apt-get update && \
    apt-get install -y curl wget && \
    rm -rf /var/lib/apt/lists/*
```

**Почему:**
- Меньше слоёв = меньше размер образа
- Очистка кеша в том же слое уменьшает размер

---

### 2. Используйте `.dockerignore`

**Всегда добавляйте в `.dockerignore`:**
```dockerignore
.git/
.venv/
__pycache__/
*.pyc
.env
node_modules/
.DS_Store
```

**Почему:**
- Уменьшает размер образа
- Ускоряет сборку
- Повышает безопасность (секреты не попадут в образ)

---

### 3. Никогда не коммитьте `.env` в Git

**Правильная настройка:**
```gitignore
# .gitignore
.env
```

```dockerignore
# .dockerignore
.env
```

**Почему:**
- `.env` содержит секреты (API ключи)
- Если закоммитить в Git, секреты станут публичными
- Если включить в образ, секреты можно извлечь из образа

**Правильный подход:**
```yaml
# docker-compose.yaml
env_file:
  - .env  # Загружается при запуске, не вшивается в образ
```

---

### 4. Используйте конкретные версии в `FROM`

**Плохо:**
```dockerfile
FROM python:latest  # Может внезапно сломаться при обновлении
```

**Хорошо:**
```dockerfile
FROM python:3.12-slim  # Фиксированная версия
```

**Почему:**
- `latest` может измениться без предупреждения
- Фиксированная версия гарантирует воспроизводимость

---

### 5. Многоэтапная сборка (Multi-stage builds)

**Для продвинутых:** Можно разделить сборку на этапы для уменьшения размера образа.

```dockerfile
# Этап 1: Сборка зависимостей
FROM python:3.12-slim AS builder
WORKDIR /app
COPY requirements.txt .
RUN pip install --user -r requirements.txt

# Этап 2: Финальный образ
FROM python:3.12-slim
WORKDIR /app
COPY --from=builder /root/.local /root/.local
COPY main.py .
COPY static/ ./static/
CMD ["python", "-m", "uvicorn", "main:app", "--host", "0.0.0.0"]
```

**Почему:**
- Убирает из финального образа инструменты сборки
- Уменьшает размер образа на 30-50%

---

### 6. Принцип наименьших привилегий

**Не запускайте приложение от root:**
```dockerfile
# Создать пользователя
RUN useradd -m appuser

# Переключиться на него
USER appuser
```

**Почему:**
- Если приложение взломают, злоумышленник не получит root доступ
- Безопасность

---

### 7. Используйте health checks

```dockerfile
HEALTHCHECK --interval=30s --timeout=3s \
  CMD curl -f http://localhost:8000/hello || exit 1
```

**Почему:**
- Docker будет автоматически проверять, работает ли приложение
- Можно настроить автоматический перезапуск при проблемах

---

## Решение проблем

### Проблема 1: Порт 8000 уже занят

**Ошибка:**
```
Error starting userland proxy: listen tcp4 0.0.0.0:8000: bind: address already in use
```

**Решение 1: Найти и убить процесс**
```bash
# macOS/Linux
lsof -ti:8000 | xargs kill -9

# Windows
netstat -ano | findstr :8000
taskkill /PID <process_id> /F
```

**Решение 2: Изменить порт**
```yaml
# docker-compose.yaml
ports:
  - "8001:8000"  # Теперь доступ на localhost:8001
```

---

### Проблема 2: Изменения кода не применяются

**Симптомы:**
- Изменили код, но контейнер показывает старую версию

**Решение:**
```bash
# Пересобрать образ
docker-compose down
docker-compose up --build
```

**Почему:**
- Код копируется в образ при сборке
- Нужна пересборка

---

### Проблема 3: Ошибка "No space left on device"

**Причина:**
- Docker образы заполнили диск

**Решение:**
```bash
# Посмотреть, сколько места занято
docker system df

# Удалить неиспользуемые образы, контейнеры, сети
docker system prune -a

# Освободить ещё больше места (удалит volumes!)
docker system prune -a --volumes
```

---

### Проблема 4: Контейнер постоянно перезапускается

**Симптомы:**
```bash
docker ps
# STATUS: Restarting (1) 2 seconds ago
```

**Решение:**
```bash
# Посмотреть логи
docker-compose logs

# Возможные причины:
# 1. Ошибка в коде (исправьте и пересоберите)
# 2. Отсутствует .env (создайте .env с нужными ключами)
# 3. Неправильная команда в CMD (проверьте dockerfile)
```

---

### Проблема 5: "Permission denied" при сборке

**Ошибка:**
```
ERROR [internal] load build context
permission denied while trying to connect to the Docker daemon socket
```

**Решение (Linux):**
```bash
# Добавить пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиниться
newgrp docker

# Или запускать с sudo
sudo docker-compose up --build
```

---

### Проблема 6: Образ слишком большой

**Симптомы:**
```bash
docker images
# my-app    latest    1.2GB
```

**Решение:**

1. **Используйте slim/alpine базовые образы:**
```dockerfile
FROM python:3.12-slim  # Вместо python:3.12
```

2. **Добавьте в `.dockerignore`:**
```dockerignore
.git/
.venv/
*.log
```

3. **Очищайте кеш в том же слое:**
```dockerfile
RUN apt-get update && apt-get install -y curl && \
    rm -rf /var/lib/apt/lists/*
```

4. **Используйте multi-stage builds** (см. "Лучшие практики")

---

### Проблема 7: Контейнер не может подключиться к .env

**Симптомы:**
- Переменные из `.env` не загружаются

**Решение:**

1. **Убедитесь, что `.env` существует:**
```bash
ls -la .env
```

2. **Проверьте `docker-compose.yaml`:**
```yaml
env_file:
  - .env  # Правильный путь
```

3. **Перезапустите контейнер:**
```bash
docker-compose down
docker-compose up
```

4. **Проверьте переменные внутри контейнера:**
```bash
docker exec websandbox-chat-app env | grep HUGGING_FACE
```

---

### Проблема 8: "ERROR: for chat-app Cannot start service"

**Ошибка:**
```
ERROR: for chat-app  Cannot start service chat-app: driver failed programming external connectivity
```

**Решение:**

1. **Перезапустите Docker:**
```bash
# macOS/Windows
# Перезапустить Docker Desktop

# Linux
sudo systemctl restart docker
```

2. **Убедитесь, что порт свободен:**
```bash
lsof -ti:8000 | xargs kill -9
```

3. **Попробуйте другой порт:**
```yaml
ports:
  - "8001:8000"
```

---

## Практические примеры для этого проекта

### Пример 1: Первый запуск проекта

```bash
# 1. Клонировать проект (или у вас уже есть)
cd /Users/gymoroz/00.MyFiles/00.Programming/Projects/WebSandBoxProject

# 2. Убедиться, что есть .env файл
cat .env
# Должно быть: HUGGING_FACE_API_KEY=hf_your_key

# 3. Собрать образ и запустить
docker-compose up --build

# 4. Открыть браузер
# http://localhost:8000
```

**Что происходит:**
1. Docker читает `docker-compose.yaml`
2. Собирает образ по рецепту из `dockerfile`
3. Создаёт и запускает контейнер `websandbox-chat-app`
4. Пробрасывает порт 8000
5. Загружает переменные из `.env`
6. Запускает `uvicorn main:app --host 0.0.0.0 --port 8000`

---

### Пример 2: Изменение кода (изменили язык логирования)

```bash
# 1. Изменили main.py
# logging.info("Requesting Hugging Face API")
# →
# logging.info("Запрос к Hugging Face API")

# 2. Пересобрать образ
docker-compose down
docker-compose up --build

# 3. Проверить в логах
docker-compose logs -f
# Должно быть: INFO: Запрос к Hugging Face API
```

**Почему пересборка:**
- Код копируется в образ при сборке (`COPY main.py .`)
- Изменения применяются только после пересборки

---

### Пример 3: Изменение API ключа

```bash
# 1. Изменили .env
# HUGGING_FACE_API_KEY=hf_старый_ключ
# →
# HUGGING_FACE_API_KEY=hf_новый_ключ

# 2. Перезапустить (БЕЗ пересборки)
docker-compose restart

# 3. Проверить, что новый ключ загружен
docker exec websandbox-chat-app env | grep HUGGING_FACE_API_KEY
# Должно быть: HUGGING_FACE_API_KEY=hf_новый_ключ
```

**Почему НЕ пересборка:**
- `.env` не копируется в образ (он в `.dockerignore`)
- Переменные загружаются при запуске через `env_file`

---

### Пример 4: Добавление новой зависимости

```bash
# 1. Активировать локальное окружение
source .venv/bin/activate

# 2. Установить новую библиотеку
pip install pydantic-settings

# 3. Обновить requirements.txt
pip freeze > requirements.txt

# 4. Пересобрать образ
docker-compose down
docker-compose up --build

# 5. Проверить, что библиотека доступна
docker exec websandbox-chat-app pip list | grep pydantic-settings
# Должно быть: pydantic-settings    x.x.x
```

**Почему пересборка:**
- Зависимости устанавливаются при сборке (`RUN pip install -r requirements.txt`)
- Нужна пересборка для установки новой библиотеки

---

### Пример 5: Просмотр логов приложения

```bash
# Запустить в фоновом режиме
docker-compose up -d

# Следить за логами в реальном времени
docker-compose logs -f

# Последние 50 строк логов
docker-compose logs --tail 50

# Логи конкретного сервиса
docker-compose logs chat-app
```

**Пример вывода:**
```
websandbox-chat-app  | INFO:     Started server process [1]
websandbox-chat-app  | INFO:     Waiting for application startup.
websandbox-chat-app  | INFO:     Application startup complete.
websandbox-chat-app  | INFO:     Uvicorn running on http://0.0.0.0:8000
websandbox-chat-app  | INFO:     Запрос к Hugging Face API
websandbox-chat-app  | INFO:     Ответ получен успешно
```

---

### Пример 6: Тестирование приложения в контейнере

```bash
# Запустить контейнер
docker-compose up -d

# Проверить, что контейнер работает
docker ps | grep websandbox

# Проверить endpoint /hello
curl http://localhost:8000/hello
# Ответ: {"message":"Hello, World!"}

# Проверить /test-api
curl http://localhost:8000/test-api
# Ответ: {"success":true,"response":"Hello! How can I assist you today?"}

# Открыть интерактивный чат
open http://localhost:8000
```

---

### Пример 7: Зайти внутрь контейнера (отладка)

```bash
# Открыть bash внутри контейнера
docker exec -it websandbox-chat-app bash

# Внутри контейнера:
pwd
# /app

ls -la
# main.py  static/  ...

python
# >>> import main
# >>> print(main.app)

exit
```

**Когда это полезно:**
- Проверить, какие файлы скопировались в образ
- Проверить переменные окружения: `env`
- Запустить Python REPL для отладки
- Посмотреть установленные библиотеки: `pip list`

---

### Пример 8: Полная очистка и пересоздание

```bash
# 1. Остановить и удалить контейнеры
docker-compose down

# 2. Удалить образ
docker rmi websandboxproject_chat-app

# 3. Очистить весь кеш Docker (опционально)
docker system prune -a

# 4. Пересобрать с нуля (без кеширования)
docker-compose build --no-cache

# 5. Запустить
docker-compose up
```

**Когда использовать:**
- Что-то пошло не так, и обычная пересборка не помогает
- Подозреваете проблемы с кешированием слоёв
- Хотите освободить место на диске

---

### Пример 9: Запуск в production режиме

```bash
# Собрать образ
docker-compose build

# Запустить в фоновом режиме
docker-compose up -d

# Проверить статус
docker ps

# Проверить логи
docker-compose logs --tail 100

# Если всё работает, настроить автозапуск
# (уже настроено через restart: unless-stopped в docker-compose.yaml)
```

**Дополнительные настройки для production:**

1. **Использовать `.env.production`:**
```yaml
# docker-compose.production.yaml
env_file:
  - .env.production
```

2. **Отключить автоперезагрузку в Dockerfile:**
```dockerfile
# Без --reload
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

3. **Добавить больше workers:**
```dockerfile
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]
```

---

## Заключение

Теперь вы понимаете:

✅ Что такое Docker и зачем он нужен
✅ Что делает каждая строка в `dockerfile`, `docker-compose.yaml` и `.dockerignore`
✅ Как работает типичный Docker pipeline
✅ Все основные команды Docker
✅ **Главное: когда нужно пересобирать контейнер, а когда нет**
✅ Лучшие практики работы с Docker
✅ Как решать типичные проблемы

**Золотое правило:**

- **Изменился код/зависимости/Dockerfile** → `docker-compose up --build`
- **Изменился `.env`** → `docker-compose restart`
- **Изменился README/другие документы** → ничего не делать

Docker — мощный инструмент, но его не нужно бояться. Начните с простых команд (`docker-compose up`, `docker-compose down`), и со временем всё станет понятным и естественным.

Удачи в работе с Docker! 🐳🚀
